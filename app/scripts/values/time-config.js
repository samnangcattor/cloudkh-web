'use strict';

/**
 * @ngdoc cloudWebApp
 * @name cloudWebApp.TimeConfig
 * @description
 * TimeConfig
 * Value in the cloudWebApp.
 */
angular.module('cloudWebApp')
  .value('TimeConfig', {
    INTERVAL: 1000,
    ONE_MINUTE: 60
  });
