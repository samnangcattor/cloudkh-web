'use strict';

/**
 * @ngdoc function
 * @name cloudWebApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the cloudWebApp
 */
angular.module('cloudWebApp')
  .controller('UserCtrl', [
    function (
    ) {
  }]);
